# CLAUDE.md — 학자금노트 (hakjagum-note)

등록금에서 **실제로 돈이 갈리는 지점** — 학자금 지원구간·국가장학금 지원액·
학자금대출 상환액·교육비 세액공제를 계산기 4종과 가이드 10편으로 정리하는
애드센스 수익형 미니사이트. 생활반장 노트 시리즈의 18번째 노트.

> 경계 주의: **세금노트**가 연말정산 전반을, **급여노트**가 재직 중 급여를,
> **청년정책노트**가 청년 지원제도를 다룬다. 이 노트는 그 셋을 다시 계산하지 않고
> **학자금** 쪽만 맡는다. 교육비 세액공제는 세금노트의 연말정산 계산기가 대표 공제만
> 다루고 교육비를 넣지 않으므로 겹치지 않는다(`tax-note/lib/year-end.ts` 주석에 명시).
> 연봉 실수령액은 급여노트로, 연말정산 전체 환급액은 세금노트로 보낸다.

## 스택·명령

- Next.js 16.2.10 (App Router) + TypeScript + Tailwind CSS 4. DB·로그인·결제·API 없음, 전부 정적.
- 개발 서버: 워크스페이스 `.claude/launch.json`의 `hakjagum-note-dev` (포트 4800)
- 빌드: `npm run build` / 테스트: `npm test` (vitest 81개)
- Node는 포터블: 명령 앞에 `$env:Path = "E:\클로드\tools\node;$env:Path"` 필요
- **빌드 성공 판정은 `$LASTEXITCODE -eq 0`으로 한다.** `✓ Compiled`는 타입 체크 전에 나온다.
- **PowerShell `Set-Content -Encoding utf8`은 BOM을 붙인다.** `package.json`에 BOM이
  들어가면 빌드가 통째로 깨진다. `[System.IO.File]::WriteAllText($f, $t, (New-Object System.Text.UTF8Encoding $false))`를 쓸 것.
- 배포: `git push origin main` (Vercel 자동 배포)만 사용. 절차는 `DEPLOY.md`
- 도메인: hakjagum.lifebanjang.com (**아직 배포 전**)
- 테마: 인디고(`--accent: #4c4192`) — 기존 18개 노트와 겹치지 않음

## 구조

- 계산 엔진 + 테스트 (로직 수정 시 반드시 테스트 함께 갱신):
  - `lib/bracket.ts` 학자금 지원구간 — **이 노트의 뿌리.** 소득인정액 =
    소득평가액 + 재산의 소득환산액 − 형제·자매 공제. `judgeBracket()`이
    `gapToLowerBracket`·`roomToUpperBracket`으로 경계까지의 거리를 함께 돌려준다.
  - `lib/scholarship.ts` 국가장학금 — 구간별 단가를 등록금으로 자른다.
    `cappedBy`가 "단가보다 등록금이 적어 잘린 금액"을 드러내는 것이 요점.
  - `lib/icl.ts` 취업 후 상환 대출 — 근로소득공제를 **양방향으로** 계산해
    소득금액 ↔ 총급여를 환산한다. `salaryFromIncome()`이 구간별 1차식의 닫힌 해.
  - `lib/deduction.ts` 교육비 세액공제 — 본인은 한도 없음, 부양가족은 한도.
    교복 50만·체험학습 30만의 항목별 한도가 300만 한도 **안에서** 먼저 걸린다.
- 가이드 10편: `lib/guides-1.ts`(지원구간·국가장학금 5편) · `lib/guides-2.ts`(대출·세액공제 5편)
- 계산기 페이지: `app/calc/{bracket,scholarship,icl,deduction}/page.tsx`

## 이 노트가 반드시 설명해야 하는 것

### ① 구간은 연봉으로 정해지지 않는다 — 전 계산기의 공통 논지
소득인정액에는 **재산의 소득환산액**이 들어간다. 4인 가구 월 소득 300만원이
재산 없으면 2구간인데, 일반재산 2억이 있으면 **4구간**이 된다. 소득은 1원도
다르지 않은데 연 지원액이 160만원 갈린다. **이 대비를 빼지 말 것** — 이 노트가
다른 국가장학금 계산기와 구별되는 지점이다.

### ② 지원금은 등록금을 넘지 못한다
"1구간이면 600만원"은 **단가**가 600만원이라는 뜻이다. 등록금 400만원인
국립대 1구간 학생은 400만원만 받고 남는 200만원은 사라진다. 등록금 900만원인
사립대 1구간 학생은 600만원을 받고 300만원을 스스로 낸다. **같은 구간인데
자기 부담이 정반대**라는 것이 핵심.

### ③ 기준은 연봉이 아니라 소득금액이다 (ICL)
상환기준소득 1,898만원은 총급여가 아니다. 근로소득공제를 역산하면 총급여
**2,851만원**이다. 출처마다 2,851만과 3,037만으로 엇갈리는데, 1,898만에
근로소득공제를 적용하면 정확히 2,851만이 나와 이쪽이 정합적이다.
`icl.test.ts`가 이 왕복 환산을 검증한다.

### ④ 학자금대출은 갚을 때 공제받는다
등록금을 낸 해가 아니라 **원리금을 상환한 해**에 본인 교육비로 넣는다.
한도가 없어 전액이 대상이고 15%가 세액에서 빠진다. 사회초년생이 가장 많이
놓치는 항목. 반대로 **국가장학금으로 낸 등록금은 넣으면 추징**된다.

## 계산하지 않는 것 (의도적)

- **국가장학금 II유형·교내장학금** — 대학이 자체 기준으로 배분해 학교마다 금액이
  다르다. 일률적으로 계산할 수 없는 것을 추정하면 잘못된 판단을 부르므로 학교
  장학팀 확인으로 안내한다. 건강보험노트가 지역가입자 보험료를 계산하지 않는 것과
  같은 이유다. **이 방침을 바꾸지 말 것.**
- **소득평가액의 항목별 공제** — 재단이 적용하는 세부 공제는 공개된 산식이 없어,
  입력한 월 소득을 그대로 소득평가액으로 본다. 그래서 결과가 실제보다 **보수적**
  (구간이 높게) 나올 수 있고, 그 사실을 계산기와 about에 밝혀 두었다.
- **주거용재산 별도 환산율** — 재단 공시에 일반·금융·자동차 셋만 있어 그대로 따랐다.

## 주의사항 — 갱신 대상

> 전 노트의 갱신 항목은 워크스페이스 `E:\클로드\CLAUDE.md`의
> **"기준값 갱신 캘린더"** 참조.

- **기준 중위소득** — **매년 8월** 보건복지부 고시, 이듬해 1월 적용.
  `lib/bracket.ts`의 `MEDIAN_INCOME`. 2026년 4인 6,494,738원(전년 대비 6.51% 인상).
- **국가장학금 지원단가** — 교육부 예산. `lib/scholarship.ts`의 `UNIT_GENERAL`·
  `UNIT_MULTI_FIRST_SECOND`. **9구간은 2026년에 신설**됐다.
- **ICL 상환기준소득** — 매년 국세청 고시. `lib/icl.ts`의 `THRESHOLD_INCOME`.
- **학자금대출 금리** — **학기마다** 교육부 고시. `LOAN_RATE` (2026년 1학기 1.7%).
- **근로소득공제 구간** — 소득세법 개정 시. `EARNED_INCOME_DEDUCTION`.
  **급여노트·세금노트의 같은 구간표와 값이 같아야 한다.**
- **교육비 세액공제율·한도** — 소득세법 제59조의4 개정 시.
- 값을 고치면 **가이드 본문과 계산기 해설의 표**도 함께 훑을 것.
  `icl.test.ts`의 "해설 표 숫자 고정" 블록이 ICL 표를 잡아 주지만, 나머지는
  `Grep`으로 옛 숫자(예: `600만`, `1,898만`)를 검색해 확인해야 한다.

## 검증에 쓸 수 있는 기준값 (테스트에 들어 있음)

- 4인 가구·월 300만·재산 없음 → **2구간** (중위소득의 46.2%)
- 같은 소득 + 일반재산 2억 → 환산액 **1,820,900원** → **4구간**
- 재산 1억을 금융으로 → 2,086,667원 / 일반으로 → 1,390,000원 (**1.5배 차이**)
- 1구간 + 등록금 400만 → **400만원** 지원, 200만원이 잘림
- 1구간 + 등록금 900만 → 600만원 지원, **자기 부담 300만원**
- 9구간 + 등록금 800만 → **100만원**만 지원
- 다자녀 셋째·8구간 → **등록금 전액**
- 총급여 4,000만 → 소득금액 2,875만 → 연 상환 **195만 4,000원** (월 162,830원)
- 총급여 2,900만 → 연 상환 **8만 4,000원** (잔액 2,000만의 이자 34만원보다 적음)
- 상환기준소득 1,898만원 ↔ 총급여 **28,505,882원**
- 학자금대출 300만 상환 → 세액공제 **45만원**
- 대학생 자녀 등록금 1,000만 → 900만까지만, **100만원 잘림**

## 그 밖에

- 브라우저 스크린샷은 이 환경에서 타임아웃 가능 — get_page_text/read_page/javascript_tool로 검증.
- PowerShell에서 `app/guide/[slug]` 경로는 `-LiteralPath` 사용. `&&` 불가.
- `components/FamilyLinks.tsx`의 SITES 배열은 전 사이트 공통 정본.
  **배포 전까지 다른 노트에 hakjagum 항목을 넣지 말 것**(게이팅).
- **가이드를 추가·수정하면** 허브에서 `npm run gen:note-guides`를 돌리고 허브도 함께
  배포해야 `lifebanjang.com/articles/hakjagum` 목록에 반영된다.
- 입력한 소득·재산·급여는 브라우저에서만 처리하고 서버로 보내지 않는다.
- **세무·금융 자문이 아니라는 면책**과 "구간과 지원액은 한국장학재단이 최종 결정"이라는
  고지를 유지할 것.
